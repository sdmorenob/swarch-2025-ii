apiVersion: v1
kind: Service
metadata:
  name: postgres-tags
  namespace: tasknotes
spec:
  type: ClusterIP
  selector:
    app: postgres-tags
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432