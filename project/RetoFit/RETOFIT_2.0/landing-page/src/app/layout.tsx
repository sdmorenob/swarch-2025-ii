import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Reto-Fit - Transforma tu Estilo de Vida",
  description: "La plataforma definitiva para crear retos de fitness, compartir tu progreso y alcanzar tus metas junto a una comunidad activa.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="es">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
