apiVersion: v1
kind: Service
metadata:
  name: postgres-tasks
  namespace: tasknotes
spec:
  type: ClusterIP
  selector:
    app: postgres-tasks
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432