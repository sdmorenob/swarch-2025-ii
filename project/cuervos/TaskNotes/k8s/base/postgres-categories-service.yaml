apiVersion: v1
kind: Service
metadata:
  name: postgres-categories
  namespace: tasknotes
spec:
  type: ClusterIP
  selector:
    app: postgres-categories
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432