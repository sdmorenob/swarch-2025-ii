########################################
# Search Service Environment Variables  #
########################################

# MongoDB connection string used by the search service (read-only access is ideal)
MONGODB_URL=mongodb://admin:password@mongodb:27017/tasknotes?authSource=admin

# HTTP port where the service listens
PORT=8081

# Environment name (development|production)
ENV=development

# Cache Aside settings
# Redis connection for caching (no host ports exposed; internal-net)
REDIS_URL=redis:6379
# TTL for cached search responses (seconds)
CACHE_TTL_SECONDS=120
# RabbitMQ URL for cache invalidation subscription
RABBITMQ_URL=amqp://rabbitmq:5672
